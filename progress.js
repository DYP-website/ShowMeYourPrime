function renderProgress(){
 const t=profileTargets(); const days=lastNDays(28); const calorieData=days.map(d=>({label:new Date(d+'T12:00').getDate(),value:sumMacros(mealsFor(d)).kcal})); const deficitData=days.map(d=>({label:new Date(d+'T12:00').getDate(),value:Math.max(0,t.maintenance-sumMacros(mealsFor(d)).kcal)})); const weightPoints=state.weights.slice(-20).map(w=>({label:w.date,value:w.weight}));
 $('#progress').innerHTML=`<div class="grid two"><div class="card"><p class="eyebrow">Progressi</p><h2>Peso</h2>${weightPoints.length?lineChart(weightPoints):'<p class="muted">Inserisci il peso dal profilo.</p>'}</div><div class="card"><h2>Nuovo peso</h2><div class="form-grid"><div class="field"><label>Data</label><input id="wDate" type="date" value="${todayISO()}"></div><div class="field"><label>Peso kg</label><input id="wVal" type="number" step="0.1" value="${state.profile.currentWeight}"></div></div><br><button class="primary" id="saveWeight">Salva peso</button></div></div><br><div class="grid two"><div class="card"><h2>Calorie assunte</h2>${barChart(calorieData,'value',Math.max(t.target,...calorieData.map(x=>x.value)))}</div><div class="card"><h2>Deficit calorico stimato</h2>${barChart(deficitData,'value')}</div></div>`;
 $('#saveWeight').onclick=()=>{ const w=Number($('#wVal').value); state.profile.currentWeight=w; state.weights.push({id:uid(),date:$('#wDate').value,weight:w}); saveState(); renderAll(); toast('Peso salvato'); };
}
